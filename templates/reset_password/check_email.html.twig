{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
	<div class="flex min-h-screen bg-gray-100 items-center justify-center px-6 py-12 lg:px-8">
		<div class="max-w-md w-full bg-white rounded-lg shadow-lg p-8">
			<p class="text-center">
				If an account matching your email exists, then an email was just sent that contains a link that you can use to reset your password.
				      This link will expire in
				{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
			</p>
			<p class="text-center mt-4">If you don't receive an email, please check your spam folder or
				<a href="{{ path('app_forgot_password_request') }}" style="color: blue;">try again.</a></p>
		</div>
	</div>

{% endblock %}